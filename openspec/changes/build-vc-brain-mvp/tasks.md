## 1. P0 Scaffold and Shared Contract Freeze

> Status convention (updated 2026-07-19): `[x]` means the task is implemented and verified in the current change worktree. It does not mean the change has been merged to `main` or pushed. Partial implementations remain `[ ]`; branch integration is tracked explicitly and `work/swe-platform` remains intentionally unmerged.

- [x] 1.1 **[P0][SWE] FIRST IMPLEMENTATION STEP:** Create `backend/` with a project-authored `pyproject.toml`, initialize `uv` and `uv.lock`, add FastAPI plus test/quality dependency groups, and verify that no generic-search, model-provider, or agent-framework dependency is present.
- [x] 1.2 **[P0][SWE]** Create `src/founderlookup/{domain,ingestion,screening,api,infrastructure}`, `tests/{unit,contract,integration,fixtures}`, a minimal FastAPI health/OpenAPI smoke test, `backend/README.md`, and safe configuration examples.
- [x] 1.3 **[P0][PAIR]** Review `CONTEXT.md`, proposal, design, and four specs together; agree temporary file ownership, require OpenSpec-first behavior changes and paired shared-contract review, and designate the SWE owner as integrator. Mark tasks complete only after implementation and verification in the current change worktree; record merge-to-`main` separately.
- [x] 1.4 **[P0][PAIR]** Freeze v0 schema shapes, lifecycle vocabularies, and version identifiers for `KnowledgeValue`, Source Artifact/Observation/Claim/Evidence, provider-neutral discovery/acquisition, Opportunity Query Plan, score and Trust factor containers, axes, Assessment Envelope, Decision Readiness, memo, Recommendation, Decision, and Pipeline Run. Leave numeric rubrics and thresholds to task 3.4 and fixture review.
- [x] 1.5 **[P0][DATA/ML]** Create deterministic golden fixtures for at least five candidate examples across at least three early-signal categories, including a cold-start founder, returning founder, cross-signal candidate, duplicate identity, and seeded contradiction with expected structured outcomes.
- [x] 1.6 **[P0][PAIR]** Add shared schema/contract tests and deterministic fake discovery, intelligence, and run adapters; require both workstreams to pass this suite before merging shared changes.
- [x] 1.7 **[P0][PAIR] PARALLEL-SPLIT POINT:** After the planning artifacts, scaffold, v0 contracts, fixtures, and contract tests are committed through the team's normal user-approved Git workflow, use `work/swe-platform` and `main` branches.

## 2. P0 SWE Platform Lane

- [x] 2.1 **[P0][SWE]** (depends on 1.4) Implement minimal SQLite persistence and private local Source Artifact storage for canonical entities, versions, Evidence, score snapshots, runs, memos, and audit events.
- [x] 2.2 **[P0][SWE]** Implement idempotent company-name-plus-PDF Application intake, signature/size checks, immutable hashes, a provider-neutral page-extraction interface with deterministic fake, explicit Unknown values, and focused interview/follow-up artifact import.
- [x] 2.2a **[P0][SWE][OWNER: DiaRar]** Implement the selected Mistral OCR 4 deck-extraction adapter with configurable default `mistral-ocr-latest`, persisted concrete returned model/page Markdown/confidence/usage metadata, bounded stateless direct OCR requests, server-side `MISTRAL_API_KEY`, and fail-closed behavior. Keep founder-private transfer disabled by default. Permit it either after explicit runtime confirmation of training-use, retention or Zero Data Retention, region, and purpose controls, or through the separately enabled demo-only hackathon risk acceptance with an explicit OCR purpose and unknown controls left honest. This task approves OCR extraction only, not investment-intelligence model or orchestration use.
- [x] 2.3 **[P0][SWE]** Implement fake-backed application services for preliminary candidate assessment, full Screening, Decision Readiness, memo, Recommendation, human Decision, and stage timing without a live model; preserve accepted stage outputs and provide bounded, authorized retry/resume from the last safe boundary.
- [ ] 2.4 **[P0][SWE]** Implement the minimum `/api/v1` command/read-model surface: Application intake plus hashed/redacted/revocable founder status; thesis; sourcing run and Outbound Candidate activation/outreach state; query/screen commands; run status/retry; one nested Opportunity detail containing subject identity, assessment, Claims/Evidence, memo, Recommendation, and timing; and append-only human Decision. Keep stable Founder, Company, Screening Case, Claim, Evidence, and memo identifiers internally without requiring standalone CRUD routes or generic relationship traversal. Add the P0 access boundary: one protected investor credential, capability-scoped founder status, protected artifact retrieval, explicit CORS allowlist, bounded intake/status rate limits, and credential/capability/sensitive-data log redaction.
- [x] 2.5 **[P0][SWE]** Implement validated Opportunity Query Plan execution against canonical data, allowlisted deterministic filters, hard/preference/No Preference and Unknown semantics, bounded results with stable simple ordering, rejection of generated SQL or provider expressions, and append-only human overrides that preserve the original rule result, actor, time, and rationale.
- [ ] 2.6 **[P0][SWE]** Finish the selected React+TypeScript (`.tsx`) and Ant Design frontend against the OpenAPI contract. Keep founder intake/status in a focused public founder shell that is never present in the investor sidebar; keep the investor shell centered on thesis+sourcing+outreach, Opportunity+Evidence, and memo+Recommendation+human Decision. Apply task-first progressive disclosure so each route first exposes the next action and decision summary, then explanation, then expandable Evidence/audit detail.
- [ ] 2.7 **[P0][SWE]** Apply the rice-paper/celadon/ink direction as a small modern neumorphic theme: normal cards, panels, inputs, buttons, tags, and navigation use zero-width borders, tonal fills, rounded geometry, and consistent paired or inset shadows. Preserve semantic controls, keyboard access, high-contrast visible focus, WCAG 2.2 AA contrast/non-color states, 320-CSS-pixel and 400%-zoom reflow, reduced-motion behavior, and a usable forced-colors mode that may restore system outlines/borders.
- [ ] 2.8 **[P0][PAIR] Integration I1:** Demonstrate a deterministic fake end-to-end path in which a minimum inbound Application enters common full Screening, while a fixture Outbound Candidate first receives only preliminary assessment, is human-activated, submits a linked fixture Application, and only then enters the same full Screening; continue both paths through Evidence, memo, and human Decision.

## 3. P0 Data/ML Foundation Lane

- [ ] 3.1 **[P0][DATA/ML]** (depends on 1.4) Define the versioned sourcing hypothesis and manually label the fixture corpus for top-result relevance, early trigger, reason to contact, source diversity/freshness, thesis outcomes, coverage, and identity result.
- [x] 3.2 **[P0][DATA/ML]** Implement baseline versioned identity resolution using stable identifiers and match Evidence, preserve aliases, represent approved merges as reversible links/events, and route ambiguous identities to human review; verify it with the duplicate and same-name fixtures.
- [ ] 3.3 **[P0][DATA/ML]** Implement and test conviction-threshold logic plus candidate-keyed preliminary Assessment Envelopes with common criteria, provisional/Unknown Founder Score, three axes, Claims/Evidence, and explicit sparse coverage.
- [ ] 3.4 **[P0][DATA/ML]** Implement versioned thesis-rule, provisional Founder Score, Claim Trust, contradiction, trend-sufficiency, and Decision Readiness rubrics and thresholds; prove reproducibility and that missing source history never decrements quality.
- [x] 3.5 **[P0][DATA/ML]** Implement the framework-neutral Query Planner interface and deterministic fake/baseline that maps one natural-language request to a validated Opportunity Query Plan with criteria, bounded retrieval requests, Unknown policies, and unresolved spans.
- [ ] 3.6 **[P0][DATA/ML]** Implement the affirmative cold-start path using work-product Evidence, an exploration/coverage-aware view, and the smallest useful next Evidence or interview request rather than exhaustive intake.
- [x] 3.7 **[P0][DATA/ML]** Define framework-neutral market, idea novelty/quality, founder dossier, adversarial validation, and memo-synthesis interfaces with structured fake outputs and tests for unsupported Claims, contradictions, presentation proxies, and citation completeness.
- [x] 3.8 **[P0][PAIR]** Record that the explicit deadline-driven human selection of Tavily supersedes the proposed Tavily-versus-Exa bake-off for P0. Preserve the honest limitation that no statistically meaningful comparative benchmark was run, keep the boundary provider-neutral, and require a later change before adding a second generic provider.
- [ ] 3.9 **[P0][DATA/ML]** Complete the OSINT source palette (developer activity, scholarly/research, patents, launches, technical-community, long-form writing, cohorts, and approved professional/social) as provider-neutral adapters with deterministic recorded transports; integrate authoritative source-specific APIs and fixtures for cross-source corroboration and duplicate identity. Record public-only, robots/terms, purpose, and data-classification limits uniformly.
- [ ] 3.9a **[P0][PAIR]** Integrate bounded public startup-hackathon and showcase sourcing. Preserve event → project → participant-display-name relationships, repository/demo links, separately acquired public pitch-deck artifacts, and source-published public contact routes with exact provenance. Missing participants/decks/contacts remain Unknown; display names are not verified founder identities, and the workflow must not hunt for private contact details or silently merge people.
- [x] 3.10 **[P0][DATA/ML]** Implement the versioned evidence-graded founder trait taxonomy and the separate builder-signal and fundability reads, weighting costly-to-fake, peer-validated signals above gameable vanity signals and excluding non-predictive attributes from founder quality; verify with cold-start, gameable-signal, folklore, and under-networked-strong-builder fixtures.
- [x] 3.11 **[P0][DATA/ML]** Implement the framework-neutral confidence-band estimator by resampling the reasoned sub-score, the snap-versus-reasoned divergence factor, and the coverage-threshold abstention; add the counterfactual identity-swap bias check and the subgroup-calibration report, and prove missing history never decrements quality.

## 4. Human Search-Provider Gate and Live Sourcing

> The human reviewer selected Tavily on 2026-07-19 and the decision is recorded in `design.md`. Completion below reflects verified work in the current change worktree; `work/swe-platform` remains intentionally unmerged and unpushed.

- [x] 4.1 **[P0][PAIR][HUMAN GATE RESOLVED]:** Preserve the task 3.8 limitations and the explicit 2026-07-19 human selection of Tavily as the single P0 generic provider. Using two generic providers still requires a later OpenSpec change.
- [x] 4.2 **[P0][PAIR]** Verify and record the Tavily rationale, direct-HTTP choice, request/content budgets, public-source allow/deny policy, and rejected Exa/no-provider alternatives in `design.md`.
- [ ] 4.3 **[P0][DATA/ML]** If 4.2 selects a generic provider, add and implement only that adapter with original-URL provenance, approved data-classification/allow-deny policy, content acquisition, request/cost/latency/failure telemetry, budgets, caching, and no-result-as-Unknown behavior. If no generic provider is selected, record the skip and use the source-specific discovery path in 4.4.
- [ ] 4.3a **[P0][DATA/ML]** Implement the selected OpenAI `gpt-5.6-luna` strict Structured Outputs stage after bounded content acquisition. Require input-backed excerpts/locators and URLs, deterministic public-URL/identity/provenance validation, explicit Unknowns, server-side credentials, `store=false`, model/prompt/schema/usage telemetry, bounded failures, and deterministic fallback. Add an opt-in real-provider E2E using only synthetic public input.
- [ ] 4.4 **[P0][DATA/ML]** Implement one human-approved source-specific live adapter—prefer GitHub unless an alternate is recorded—for authoritative activity Evidence independent of any generic provider; when no generic provider is selected, it must also support the bounded P0 discovery demonstration.
- [ ] 4.5 **[P0][SWE]** Integrate the approved live adapters into bounded asynchronous sourcing runs and a thin LangGraph identify → retrieve/structure → assess-gaps → converge loop. Enforce round/request/page/time/cost budgets; persist queries, Evidence deltas, failures, and stop reason; expose Queued/Running/Succeeded/Partially Succeeded/Failed status; preserve successful records across authorized retry/resume; and provide one configurable recurring/cron-compatible trigger without requiring a production scheduler.
- [ ] 4.6 **[P0][PAIR] Integration I2:** Demonstrate a corpus spanning at least three signal categories, with at least one category and candidate collected live through the approved adapter path. Include two successive recurring-trigger runs that preserve history without duplicating unchanged artifacts, one cross-signal candidate, one duplicate-resolution result, Evidence locators, a reason to contact, an editable outreach draft, and a partial/no-result case. Include at least one public hackathon/showcase example whose event, project, participant display names, repository/demo, and public deck relationships remain inspectable without asserting an unverified identity.

## 5. Resolved P0 Orchestration and Optional Live-Model Gate

- [ ] 5.1 **[P0][DATA/ML][HUMAN GATE RESOLVED]** Preflight the human-selected OpenAI `gpt-5.6-luna` Responses API for credential access, `store=false`, purpose, bounded cost/usage, latency, refusal/failure behavior, and strict structured-output reliability using synthetic input. Record the human reviewer's explicit hackathon-MVP approval for founder-private processing behind a separate runtime enablement.
- [ ] 5.2 **[P0][PAIR][ORCHESTRATION DECISION RESOLVED]:** Use a thin LangGraph `StateGraph` for the bounded outbound retrieval loop, matching the inbound orchestration choice, while keeping provider ports, validators, deterministic query/thesis execution, and intelligence responsibilities framework-neutral. Do not grant the graph autonomous outreach, Decision, or fund-transfer authority.
- [x] 5.3 **[P0][PAIR]** Record the selected thin-LangGraph boundary, bounded convergence and audit rules, rejected broader-agent alternatives, selected GPT-5.6 Luna boundary, deterministic validation/fallback, explicit private-data runtime enablement, and prohibition on autonomous Decision/outreach/fund-transfer authority in `design.md`.
- [ ] 5.4 **[P0][DATA/ML]** Add the minimum direct-HTTP OpenAI Responses API configuration for GPT-5.6 Luna, keep `OPENAI_API_KEY` server-side, bound requests and telemetry, and retain deterministic adapters as an operational fallback.

## 6. P0 Model-Backed Intelligence

- [ ] 6.1 **[P0][DATA/ML]** Implement the approved Query Planner adapter as a schema-constrained call or justified bounded agent, preserving unresolved subjective phrases and leaving deterministic execution to the SWE-owned executor.
- [ ] 6.2 **[P0][DATA/ML]** Implement market, idea novelty/quality, and founder-dossier analyses with cited supporting/counter Evidence, confidence, gaps, and founder-presentation bias prohibitions.
- [ ] 6.3 **[P0][DATA/ML]** Implement adversarial validation, external corroboration, contradiction detection, Claim-level Trust factors, and fail-closed handling for malformed or unsupported output.
- [ ] 6.4 **[P0][DATA/ML]** Implement cited memo and Recommendation synthesis with the five required sections, explicit gaps, immutable revisions, and structured audit summaries without private chain-of-thought.
- [ ] 6.5 **[P0][SWE]** Integrate live intelligence behind existing interfaces and run resources so API and UX contracts remain unchanged from deterministic fakes.
- [ ] 6.6 **[P0][DATA/ML]** Run and document the evaluation harness for query-plan correctness, cold-start bias, hard-filter authority, score monotonicity, source silence, contradictions, citation completeness, structured-output validity, and repeatability.
- [ ] 6.7 **[P0][DATA/ML]** Wire the confidence-band resampling, snap-versus-reasoned divergence, and identity-swap bias checks into the live founder, market, and idea analyses without changing the Assessment Envelope contract.
- [ ] 6.8 **[P0][DATA/ML]** Run and document the hold-out predictive-validity evaluation (rank agreement, confidence-band coverage, capital-raised baseline, per identifiable subgroup) as the Area of Research 3 writeup.
- [ ] 6.9 **[P0][DATA/ML]** Write up the confidence-scoring method (reasoned-sample dispersion, snap-versus-reasoned divergence, and calibration) as the Area of Research 1 deliverable, including the honest logprob and small-ensemble caveat.

## 7. P0 Joint Hackathon Slice

- [x] 7.1 **[P0][PAIR] Integration I3:** Demonstrate direct inbound and activated-then-applied outbound records converging on the same full Screening contract while preserving preliminary history and origin.
- [ ] 7.2 **[P0][PAIR] Integration I4:** Demonstrate cold-start positive Evidence and focused follow-up, three-axis disagreement, a blocking contradiction, partial provider failure, founder-status access, and human-controlled outreach.
- [ ] 7.3 **[P0][PAIR]** Verify the Decision Readiness policy, required memo sections, Claim citations/Unsupported states, Recommendation versus human Decision, stage timing, and result against the 24-hour target.
- [ ] 7.4 **[P0][PAIR]** Run backend formatting/lint/type/tests, provider contract tests, API end-to-end tests, frontend checks, P0 authorization/private-artifact/rate-limit/CORS/log-redaction security checks, and targeted keyboard, contrast, 320-CSS-pixel/400%-zoom, forced-colors, reduced-motion, and semantic-state acceptance checks across the four demo surfaces; resolve P0 failures.
- [ ] 7.5 **[P0][PAIR]** Update setup and the `.env.example` contract, source/data policy, architecture, founder-versus-investor information architecture, progressive-disclosure behavior, demo script, score caveats, provider/framework decisions, known limitations, and the Recommendation/Decision/outreach/fund-transfer distinctions.
