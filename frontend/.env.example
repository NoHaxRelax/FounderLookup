# Optional non-secret URL for a host application that injects HttpFounderLookupClient.
# App defaults to deterministic fixtures; this file never selects a runtime automatically.
VITE_API_BASE_URL=http://localhost:8000/api/v1
